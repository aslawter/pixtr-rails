<li><%= link_to group.name, group %> -- <%= group.description %>
  <% if current_user.member?(group) %>
   <%= button_to "Leave this group", [group, :group_membership], method: :delete %></li>
  <% else %> 
    <%=button_to "Join this group", [group, :group_membership] %>
  <% end %>
</li>
