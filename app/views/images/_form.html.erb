<%= form_for [@gallery, @image] do |form| %> 
	<%= render "error_messages", target: @image %>
  <%= form.label :url, "URL:" %>
  <%= form.text_field :url %>
  <%= form.submit "Add new image" %> 
<% end%>
