<%= form_for @image, url: "/galleries/#{@gallery.id}/images" do |form| %>
	<div>
		<%= form.label :url, "URL:" %>
		<%= form.text_field :url %>
	</div>
	<div>
		<%= form.submit "Add new image" %>
	</div>
<% end%>